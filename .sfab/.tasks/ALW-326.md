---
id: task_0y7Q5lrDa7FAAkmN0iIjs
number: 326
display_id: ALW-326
title: "Scaffold the knowledge layer + Tier-1 primitive skeleton (ADR-0010 §E/§H)"
status: merged
kind: deliverable
priority: high
needs_walkthrough: false
created: 2026-06-18T16:45:38.580Z
updated: 2026-06-19T04:44:08.596Z
comment_count: 0
parent_task_id: task_KhzVQHNrUuhvws2eB2d51
assignee_id: i2dpBuXJnKvwD1yro1adgfw5hJ58i5Gv
---

# ALW-326 Scaffold the knowledge layer + Tier-1 primitive skeleton (ADR-0010 §E/§H)

## Context

The template architecture is settled in **ADR-0010 (Amendment 2026-06-18)** and **ADR-0017**, and the **directory/package structure is decided in [[ALW-327]] (D1–D10)**. This task lays down the *structure* those imply — the **directory/package layout + renames**, the **knowledge layer** (canonical `docs/`, droppable `apps/docs` renderer, `.agents/skills` + runtime symlinks, `AGENTS.md`), and a **skeleton** for the Tier-1 base primitives — **without authoring content or implementing primitives**. The repo already has much of this; the job is to reshape it to match the decisions and add the missing scaffolding.

> **Knowledge-layer demarcation:** the docs-structure exploration is complete (decisions **E1–E4**) and the **content + conventions** half is owned by **[[ALW-330]]** (architecture.md, ADR cleanup/re-enumeration, `template.md`, `AGENTS.md` router content, `CLAUDE.md` symlink, `guides/` naming). THIS task creates only the **empty structure** the content lands in.

## Scope

### A. Directory & package structure (implements [[ALW-327]] D1–D10)
- **Renames (role over technology):** `apps/web-tanstack`→`apps/web`; `packages/db-d1`→`packages/db`; `packages/types`→`packages/contract`; `packages/cloudflare-env`→`packages/env`; `packages/typescript-config`→`packages/tsconfig`. Keep workspace scope `@workspace/*`. Update `cf-typegen` path coupling for the `env` rename. Remove any `primitives` package.
- **`contract` becomes a pure boundary-zod leaf:** no workspace deps; **remove the drizzle→zod derivations** currently in `db-d1/src/types/*` (the `createInsertSchema(...).omit(...)` files). Boundary zod is hand-written, feature-keyed (`contract/<cap>/`).
- **Layer model + cross-layer feature-key consistency:** `db` (schema one-file-per-capability + client + `$infer`) · `contract` (`<cap>/`) · `core` (queries + domain fns, `<cap>/`; params `z.infer<contract>`) · `apps/web` surfaces (`hono/{public,protected,org-protected}/<cap>/`, MCP, AI DOs) · `packages/agent/tools/<cap>.ts` · `apps/web/src/components/<cap>/`. Restructure `core` flat→`<cap>/` folders (e.g. `core/items.ts`+`core/locations.ts`→`core/catalog/`) so the `<cap>` key is identical across layers.
- **Type model rules:** ISO `text` timestamps everywhere for domain tables (fix the `mode:"timestamp"/"timestamp_ms"` stragglers; Better-Auth `Date` columns are an accepted internal-only one-off); money = numeric→`number` customType.
- **Mechanical boundaries (D8):** set each `package.json`'s deps to the allowed graph — `contract` (no workspace deps) · `db` (leaf) · `core` = db+contract · `ui` = contract (NOT core/db) · `apps/web` = core+contract+ui. Add a CI **cycle check** (`madge --circular` or equivalent).
- **Lint:** drop **Ultracite**; keep Biome with a repo-owned config (the concrete ruleset is a separate deferred task — wire the empty/own config here, don't author the full ruleset).

### B. Knowledge layer — STRUCTURE only (ADR-0010 §H; conventions in [[ALW-330]] / E1–E4)
- **Canonical docs at repo root.** `docs/` is the single source of truth with the E2 buckets: **`docs/architecture.md`**, **`docs/decisions/`**, **`docs/guides/`** (the renamed `template-best-practices/`). If markdown lives in `apps/docs/content/`, move it to root `docs/` and make `apps/docs` a **thin renderer** (no duplicated content). *(The `template-best-practices/`→`guides/` rename + its index/content is executed by [[ALW-330]]; here just ensure the buckets exist.)*
- **No `docs/plans/`** (E2): transient plans live in the SFAB task system / plan-mode, never `docs/`.
- **Droppable docs app.** Record the rule: drop the `apps/docs` *app* at fabrication, **keep `docs/`**.
- **Skills home.** `.agents/skills/` canonical; committed `.claude/skills` + `.agent/skills` symlinks resolve to it. Add one placeholder template-architecture skill whose `SKILL.md` is a thin trigger that **points into `docs/` by plain path**.
- **`AGENTS.md` exists as the Tier-1 entry skeleton** (E3); its **router content + the `CLAUDE.md`→`AGENTS.md` symlink are authored in [[ALW-330]]**. Don't duplicate that here.

### C. Tier-1 primitive skeleton (ADR-0010 §E)
- Lay out folders/stubs for the Tier-1 primitives where they live (db schema / contract / core / surfaces / components) — directories + minimal stubs that **typecheck**, not implementations. Just enough that the parts list is legible.
- **Manifest shape.** Ensure `.sfab/template.json` can carry the `packs` provenance field (ADR-0017 §3) — shape only, no behavior.

## Acceptance criteria

- [ ] AC-1: Packages/apps renamed per the role-over-tech rule; `@workspace/*` imports updated; `cf-typegen` updated for `env`; no `primitives` package. verify: tree + `pnpm typecheck`.
- [ ] AC-2: `packages/contract` is a pure boundary-zod leaf (no workspace deps, no drizzle-zod); the `db/src/types/*` derivations are removed. verify: `contract` package.json deps; grep shows no `createInsertSchema`/`.omit(` deriving from drizzle.
- [ ] AC-3: `core` restructured to `<cap>/` folders matching `hono/.../<cap>/` + `components/<cap>/`; `db` schema is one file per capability. verify: tree.
- [ ] AC-4: Domain timestamps are ISO `text`; money uses a numeric→number customType. verify: grep schema for `mode:"timestamp"` (only the accepted auth one-off remains).
- [ ] AC-5: The `package.json` dep-graph enforces the layer boundaries (an illegal cross-layer import fails resolution) and a CI cycle-check is present. verify: attempt `import "@workspace/core"` from `ui` fails; cycle-check runs in CI.
- [ ] AC-6: Ultracite removed; Biome runs with the repo-owned config. verify: `biome.jsonc` has no ultracite extend; `pnpm lint:check` passes.
- [ ] AC-7: Canonical `docs/` at repo root with the E2 buckets (`architecture.md`, `decisions/`, `guides/`, no `plans/`); `apps/docs` renders from it with no duplicated content; fabrication drops the app, keeps `docs/`. verify: `pnpm --filter docs build`; fabrication marker lists `apps/docs` to remove, `docs/` to keep. (Doc *content* lands in [[ALW-330]].)
- [ ] AC-8: `.agents/skills/` canonical; `.claude/skills` + `.agent/skills` committed symlinks; a placeholder skill references a doc by plain path; `AGENTS.md` exists as the Tier-1 entry skeleton. verify: `ls -la .claude/skills`; `SKILL.md` has a `docs/...` path mention. (Router content + `CLAUDE.md` symlink land in [[ALW-330]].)
- [ ] AC-9: Tier-1 primitive skeleton folders/stubs exist and typecheck, matching ADR-0010 §E; `.sfab/template.json` carries the `packs` provenance shape. verify: tree vs parts list; type of `template.json`.
- [ ] AC-10: `pnpm typecheck && pnpm lint:check && pnpm test && pnpm build` pass. verify: run all four.

## Non-goals

- Authoring real docs content (architecture writeup, ADR cleanup/re-enumeration, `AGENTS.md` router content, `CLAUDE.md` symlink, `guides/` rename) — owned by **[[ALW-330]]** (the docs-structure exploration is complete).
- Authoring real skills content — separate.
- Implementing any Tier-1 primitive (Transaction Core, activity log, i18n) — separate build tasks.
- Authoring the full Biome ruleset (only wire the repo-owned config) — separate task.
- The registry/pack mechanism behavior (`registry.json`, install skills, manifest *behavior*) — [[ALW-324]] / ADR-0017.
- De-verticalizing the example beyond the structural renames/restructure — [[ALW-299]].

## Boundaries

- ✅ Always: structure only; stubs must typecheck; match [[ALW-327]] D1–D10 + ADR-0010 §E/§H + ADR-0017 §6 + E1–E4 ([[ALW-330]]).
- ✅ Always: skills reference docs by path mention; symlinks only for runtime exposure of `.agents/skills`.
- ⚠️ Ask first: deleting/relocating existing docs content beyond the `docs/` move; changing how the template is tagged/released.
- 🚫 Never: author doc/skill content or the ADR re-enumeration (that's [[ALW-330]]); author primitive implementations; reintroduce a `types`/`primitives` package or a drizzle→zod chain; couple anything to a vertical's domain naming; create `docs/plans/`.

## References

- [[ALW-327]] — directory/package structure decisions (D1–D10); this implements them 1:1.
- [[ALW-330]] — docs **content + conventions** (E1–E4): architecture.md, ADR cleanup/re-enumeration, `AGENTS.md` router, `CLAUDE.md` symlink, `guides/`. This task builds the empty structure it fills.
- ADR-0010 §E (parts list), §H (knowledge layer); ADR-0017 §3 (manifest), §6 (repo layout).
- Starter ADRs touched by the renames/structure — **subject to re-enumeration by [[ALW-330]]**: current `001` (monorepo), `004` (`cloudflare-env`→`env`), `005` (drizzle-zod removal).
- [[ALW-323]] Group · [[ALW-299]] seams · [[ALW-324]] registry.
- Existing: `apps/docs/`, `.agents/skills/`, `.claude`/`.agent` symlinks, `AGENTS.md`.
