---
id: task_VgdJyzZbdXBNXrTm_HK2h
number: 856
display_id: ALW-856
title: "Starter stale copy + voice STT key: \"Acme Inc.\" manifest, \"ERP Assistant\" label, undocumented OPENAI_API_KEY"
status: draft
kind: deliverable
priority: medium
created: 2026-09-01T01:44:30.565Z
updated: 2026-09-01T01:44:30.565Z
comment_count: 0
---

# ALW-856 Starter stale copy + voice STT key: "Acme Inc." manifest, "ERP Assistant" label, undocumented OPENAI_API_KEY

## Context
Leftovers that would ship inside a fabricated customer app: `app_name` / PWA manifest still say "Acme Inc.", the chat is labeled "ERP Assistant", and `ChatVoiceButton` + `POST /transcribe` use `@ai-sdk/openai` `whisper-1` with no `OPENAI_API_KEY` in `.dev.vars.example` or the inference guide — the mic silently fails for anyone without owner knowledge. Second of the first Factory loop jobs.

## Scope
- Replace "Acme Inc." with a neutral placeholder driven by the org/app name; make the chat label neutral and translatable (EN + ES).
- Voice STT: either document `OPENAI_API_KEY` in `.dev.vars.example` + the inference guide and hide the button when the key is absent, or remove the button. Recommendation: document + hide when absent.
- Check `nav_platform` grouping and glossary wording that still describe verb-first routes "if/when they ship".

## Acceptance criteria
- [ ] AC-1: No "Acme Inc." string in `apps/web` or `packages/i18n`. verify: `rg "Acme"`
- [ ] AC-2: Chat label is a translated message key with EN + ES values, not "ERP Assistant" hardcoded.
- [ ] AC-3: With no `OPENAI_API_KEY`, the mic button is not rendered and `/transcribe` returns a clear 4xx; with the key, transcription works. verify: run locally both ways
- [ ] AC-4: `.dev.vars.example` and the inference guide list every key the app can use.
- [ ] AC-5: Starter CI green (incl. `i18n:lint`).

## Non-goals
The voice-lab TTS/Flux work (separate packet). IA changes (separate task).

## Boundaries
- ✅ Always: keep EN + ES in sync; one PR.
- ⚠️ Ask first: removing the voice button instead of gating it.
- 🚫 Never: commit real keys.

## References
- Sweep: `result-templates.txt` §3 (private-pass table), §5
