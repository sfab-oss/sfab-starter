---
id: task_dR-W_w2eFGXXWo4bs4PNp
number: 495
display_id: ALW-495
title: "Starter: own Biome ruleset, drop Ultracite, bump Biome"
status: merged
kind: deliverable
priority: medium
needs_walkthrough: false
created: 2026-07-10T18:01:19.169Z
updated: 2026-07-10T18:58:59.454Z
comment_count: 1
parent_task_id: task_UzXjI_QTeAYFb60mKKJxU
last_comment_at: 2026-07-10T18:10:12.378Z
assignee_id: i2dpBuXJnKvwD1yro1adgfw5hJ58i5Gv
---

# ALW-495 Starter: own Biome ruleset, drop Ultracite, bump Biome

## Context

Closes the deferred half of starter D8 / ALW-326 AC-6: stop extending Ultracite; own the Biome config; bump Biome. Parent Group: [[ALW-494]]. Platform port is a follow-on deliverable under the same Group. **Smell / `useEffect` lint gates are out of this deliverable** — later child [[ALW-496]].

## Scope

- Remove `ultracite` dependency; root config must not `extends` Ultracite.
- **Package (settled):** add `packages/biome-config` as `@workspace/biome-config` (same idea as `@workspace/tsconfig`), exporting forked presets:
  - `./core`, `./react`, `./tanstack` → Biome jsonc fragments copied from **latest Ultracite (7.9.3+)**.
  - Root `biome.jsonc` extends those three + starter-specific overrides (e.g. `noBarrelFile: off`).
- **Drop `next`** — Next-only rules; Ultracite's TanStack answer is the `tanstack` preset (routes `useFilenamingConvention` off).
- Bump `@biomejs/biome` to current latest at implementation (today 2.5.x); fix fallout.
- **Trim depth (settled):** parity exit — green on fork + bump; only lighten what the bump breaks or what we already override. No deep rule-by-rule redesign.
- Implement in a packet worktree from latest `origin/main` (`active/alw-495-…`, branch `alw-495-…`) — not the canonical checkout.

## Acceptance criteria

- [ ] AC-1: `ultracite` gone from package.json / lockfile; no ultracite extend. verify: grep + `pnpm lint:check`
- [ ] AC-2: `@workspace/biome-config` exists with `core` / `react` / `tanstack` exports; root extends them (not `next`). verify: package exports + root `biome.jsonc`
- [ ] AC-3: `@biomejs/biome` bumped to agreed target; lint passes. verify: package.json + `pnpm lint:check`
- [ ] AC-4: Intentional overrides preserved (barrels, etc.). verify: root `biome.jsonc`
- [ ] AC-5: Full gate suite green. verify: `pnpm typecheck && pnpm lint:check && pnpm test`

## Non-goals

- Smell / `useEffect` lint gates — [[ALW-496]].
- Platform (`sfab`) port — separate child of [[ALW-494]].
- Converting Next-specific Ultracite rules into Start analogues.
- Published cross-repo biome-config package.

## Boundaries

- ✅ Always: PR-only; worktree from latest main; keep formatter behavior stable unless an intentional trim.
- ⚠️ Ask first: turning a previously-error Ultracite rule off if it silences a large existing baseline.
- 🚫 Never: reintroduce Ultracite; push to main; edit the canonical checkout.

## References

- [[ALW-494]] · [[ALW-496]] · [[ALW-327]] D8 · [[ALW-326]] AC-6 deferral
- Ultracite 7.9.3: `config/biome/{core,react,tanstack}/biome.jsonc`

## Discussion

**🤖 mcp** · agent · 2026-07-10 18:10

> Local loop started: packet `active/alw-495-own-biome-config`, worktree from `origin/main` @ `c52198e`, branch `alw-495-own-biome-config` (edit-only). Dispatching grok-4.5-high implementer for the Ultracite → `@workspace/biome-config` fork + Biome bump. Smell gates stay on ALW-496.

