---
id: task_dR-W_w2eFGXXWo4bs4PNp
number: 495
display_id: ALW-495
title: "Starter: own Biome ruleset, drop Ultracite, bump Biome"
status: draft
kind: deliverable
priority: medium
needs_walkthrough: false
created: 2026-07-10T18:01:19.169Z
updated: 2026-07-10T18:03:31.565Z
comment_count: 0
parent_task_id: task_UzXjI_QTeAYFb60mKKJxU
---

# ALW-495 Starter: own Biome ruleset, drop Ultracite, bump Biome

## Context

Closes the deferred half of starter D8 / ALW-326 AC-6: stop extending Ultracite; own the Biome config; bump Biome. Parent Group: [[ALW-494]]. Platform port is a follow-on deliverable under the same Group. **Smell / `useEffect` lint gates are explicitly out of this deliverable** — they need a justify-the-baseline pass and land as a later child of [[ALW-494]].

## Scope

- Remove `ultracite` dependency; `biome.jsonc` must not `extends` Ultracite.
- **Fork strategy (settled):** copy Ultracite's `core` + `react` presets into a repo-owned config (drop `next` — TanStack Start, not Next). Use the local copy to analyze keep/trim.
- Bump `@biomejs/biome` to current latest at implementation (today 2.5.x); fix fallout.
- Preserve intentional starter overrides (e.g. `noBarrelFile: off` for capability barrels).
- `pnpm lint:check` / typecheck / test green.

## Acceptance criteria

- [ ] AC-1: `ultracite` gone from package.json / lockfile; `biome.jsonc` has no ultracite extend. verify: grep + `pnpm lint:check`
- [ ] AC-2: `@biomejs/biome` bumped to agreed target; lint passes on the forked ruleset. verify: package.json + `pnpm lint:check`
- [ ] AC-3: Intentional overrides preserved (barrels, route filename convention, etc.). verify: inspect `biome.jsonc`
- [ ] AC-4: Full gate suite green. verify: `pnpm typecheck && pnpm lint:check && pnpm test`

## Non-goals

- Smell / `useEffect` lint gates and justify-the-baseline ignores — later child of [[ALW-494]].
- Platform (`sfab`) port — separate child of [[ALW-494]].
- Shared published biome-config package.
- Encoding non-AST smells (DB migrations, etc.).

## Boundaries

- ✅ Always: PR-only; keep formatter behavior stable unless an intentional trim.
- ⚠️ Ask first: turning a previously-error Ultracite rule off if it silences a large existing baseline.
- 🚫 Never: reintroduce Ultracite; push to main.

## References

- [[ALW-494]] Group · [[ALW-327]] D8 · [[ALW-326]] AC-6 deferral
- Ultracite presets under `node_modules/ultracite/config/biome/{core,react,next}/`
