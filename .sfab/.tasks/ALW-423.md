---
id: task_njdHBHlnP9JxNG09nmXs0
number: 423
display_id: ALW-423
title: "QA: shell, navigation & command-K palette (desktop + mobile)"
status: draft
kind: deliverable
priority: high
needs_walkthrough: true
created: 2026-07-08T20:02:40.817Z
updated: 2026-07-08T20:02:40.817Z
comment_count: 0
---

# ALW-423 QA: shell, navigation & command-K palette (desktop + mobile)

## Context

The global chrome is the highest-traffic surface: sidebar (`components/layout/app-sidebar.tsx`), org switcher / account footer (`app-sidebar-footer.tsx`), breadcrumbs, theme toggle, and the ⌘K search palette (`components/search/search-command.tsx`). The palette is known to be a stub — static 3-page list (Documents missing), hardcoded `isLoading = false`, unwired debounce — and the org switcher shows ⌘1..N shortcut labels that are not bound. First dogfood pass fixed mobile sidebar auto-close and breadcrumb doubling ([[ALW-419]]); those fixes need regression verification.

## Scope

- Sidebar: expand/collapse rail, active states, mobile sheet open/auto-close-on-nav, trigger reachability.
- Breadcrumbs on every route incl. leaf duplication regression (Settings, Today, invoice routes).
- Org switcher: switch org (chat dock must remount for the new org), create org entry, log out; verify ⌘1..N labels vs actual behavior.
- Command palette: ⌘K, Ctrl-K, and `/` open/close/toggle; guard when focus is in inputs/textareas/contenteditable; keyboard-only navigation and Enter-to-open; result coverage vs actual pages; empty state.
- Theme toggle from both variants (icon and menu row).

## Acceptance criteria

- [ ] AC-1: Every behavior in Scope exercised on desktop and mobile viewports with screenshot/video evidence in the QA packet artifacts.
- [ ] AC-2: ALW-419 regressions verified fixed (mobile sidebar auto-close, breadcrumb doubling) with evidence.
- [ ] AC-3: Palette gaps confirmed and enumerated (missing Documents entry, no entity search, dead loading scaffold, unbound ⌘1..N) with a written recommendation feeding the palette feature task.
- [ ] AC-4: Every confirmed finding is logged in the session tracker and filed as a draft fix task or folded into an existing one.

## Non-goals

- Implementing real palette search (separate feature task).
- Fixing any finding in this session.

## Boundaries

- ✅ Always: local dev runtime, seeded data, evidence artifacts per finding.
- ⚠️ Ask first: filing more than ~5 new fix tasks (batch instead).
- 🚫 Never: code changes on this task's branch; prod/remote.

## References

- `apps/web/src/components/search/search-command.tsx`, `apps/web/src/components/layout/*`
- Workspace tracker: `starter-dogfood-qa/ISSUES.md` (M-1, B-2)
- [[ALW-419]] merged fixes
