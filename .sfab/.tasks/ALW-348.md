---
id: task_k4Dg8lZNnVObjvDRE0PHZ
number: 348
display_id: ALW-348
title: "Codemode tool approvals: adopt the supported durable-approval pattern (revisit needsApproval)"
status: merged
kind: deliverable
priority: medium
needs_walkthrough: true
created: 2026-06-21T04:02:56.089Z
updated: 2026-07-08T06:29:23.010Z
comment_count: 2
last_comment_at: 2026-07-08T06:09:33.146Z
---

# ALW-348 Codemode tool approvals: adopt the supported durable-approval pattern (revisit needsApproval)

## Context

The agent exposes ERP tools to the model through **codemode** — `createExecuteTool({ tools, loader })` in `packages/agent/src/org/chat/org-chat.ts:272`, on `@cloudflare/think@0.8.5` + `@cloudflare/codemode@0.3.8`.

While building RBAC ([[ALW-347]]) we discovered a latent trap: in our installed version, `createExecuteTool` → legacy `createCodeTool` → `filterTools()`, which **silently strips out any tool whose `needsApproval` is `true` or a function** (verified in `@cloudflare/codemode/dist/resolve.js` `filterTools`, used by `createCodeTool` in `dist/ai.js`). So an approval-gated tool never appears in the sandbox at all — the model can't call it, and no error is surfaced. Our catalog write tools all carried `needsApproval: true`, which meant they were invisible to the agent. ALW-347 **dropped `needsApproval`** from those tools and relies on an in-`execute` RBAC `assertCan()` throw instead (autonomous, RBAC-gated — no human approval). That unblocked the agent but means we currently have **no human-in-the-loop approval** on any agent write.

Cloudflare's official guidance (cloudflare/agents repo, `docs/codemode/approvals.md`, `docs/codemode.md:425-429`, `packages/codemode/README.md`, as of upstream 2026-06-17) is **version-dependent**:
- **Legacy `createCodeTool` (our current 0.3.8):** "Tool approval (`needsApproval`) is not supported yet … excluded from codemode … use approval-required tools through standard AI SDK tool calling instead." → keep approval tools **top-level**, out of codemode.
- **Newer durable runtime / connector path:** `createCodemodeRuntime` + connectors mark sensitive tools `requiresApproval: true`; a gated call **durably pauses** the sandbox run and resumes via `runtime.approve({ executionId })` (Think exposes `pendingExecutions()` / `approveExecution()`). `ToolSetConnector` maps AI SDK `needsApproval` → `requiresApproval` instead of stripping it. **This path does not exist in our pinned 0.3.8 dist** (no `createCodemodeRuntime` / `ToolSetConnector` / `requiresApproval` — confirmed).

Note: Cloudflare's own docs are **internally inconsistent right now** (`codemode.md` still says approvals are "planned" while `codemode/index.md` + `approvals.md` document them as shipped) — the feature is mid-rollout, so anything here must be verified against the exact version we land on.

This task is to determine and implement the **correct, supported** way to do agent-tool approvals for this codebase — so that when a write genuinely warrants human confirmation (e.g. `delete-product`, or future money/document actions), we have a real, non-silent mechanism.

## Scope

- Audit the current codemode wiring and confirm the exact stripping behaviour on the installed version (`org-chat.ts` getTools, `createExecuteTool`, `filterTools`).
- Evaluate **upgrading** `@cloudflare/think` + `@cloudflare/codemode` to a version that ships the runtime/connector approval path (`createCodemodeRuntime` / `ToolSetConnector` / `requiresApproval` / `runtime.approve`). Assess breaking changes vs current `createExecuteTool` usage.
- Decide, per tool class, the approval model: **durable in-codemode approval** (`requiresApproval`, pause/approve/resume) vs **top-level standard AI-SDK approval tool** (out of codemode) vs **autonomous + RBAC `assertCan`** (no approval). Document the rule.
- Implement the chosen pattern and wire the **pending / approve / reject** surface into the agent chat UI (approve/reject affordances driven by `pendingExecutions()` / runtime pending, not the raw transcript).
- Add a test proving an approval-gated call actually **pauses and then resumes** (or is correctly held top-level) — closing the gap that hid the original silent-strip bug.

## Acceptance criteria

- [ ] AC-1: The exact approval behaviour of the installed think/codemode version is confirmed and written down (does `needsApproval` strip, pause, or error?). verify: cite the dist source + a test.
- [ ] AC-2: A decision is recorded on whether to upgrade think/codemode for the durable-approval runtime, with the breaking-change assessment. verify: decision note in the PR / task.
- [ ] AC-3: A documented per-tool-class rule exists for choosing durable-approval vs top-level-approval vs autonomous+RBAC. verify: read the doc/comment.
- [ ] AC-4: At least one sensitive write is approval-gated end to end — the agent run pauses at the gated call and resumes only after explicit approval (or is correctly exposed top-level with the standard approval flow). verify: test + manual agent run.
- [ ] AC-5: The approval state surfaces in the agent chat UI (pending → approve/reject), not silently dropped. verify: UI review.
- [ ] AC-6: `pnpm typecheck && pnpm lint:check && pnpm test` pass. verify: run them.

## Non-goals

- Re-deciding RBAC itself — `assertCan` / role-rank from [[ALW-347]] stays; approvals are orthogonal to authorization.
- Building the Transaction Core money/document surfaces — this task provides the approval mechanism they may consume, not the screens.
- Changing the platform repo — tracked separately in the companion platform task.

## Boundaries

- ✅ Always: verify approval behaviour against the exact installed version's source, not docs alone (docs are mid-rollout and inconsistent).
- ✅ Always: keep `assertCan` RBAC enforcement regardless of the approval mechanism chosen.
- ⚠️ Ask first: upgrading `@cloudflare/think` / `@cloudflare/codemode` major/minor (it changes the agent execution path); adding a human-approval requirement to a tool that is currently autonomous.
- 🚫 Never: reintroduce `needsApproval: true` on a codemode tool without confirming the installed version pauses (rather than silently strips) it.

## References

- `packages/agent/src/org/chat/org-chat.ts:272` — `createExecuteTool({ tools: erpTools, loader })`.
- `packages/agent/src/tools/catalog/products.ts`, `warehouses.ts` — where `needsApproval` was dropped in [[ALW-347]]; `assertCan("catalog:write", ctx)` now guards writes.
- `@cloudflare/codemode/dist/resolve.js` `filterTools` (strips `needsApproval`); `dist/ai.js` `createCodeTool`; `dist/run-code.js` (re-throws `Code execution failed: …`).
- Cloudflare guidance: cloudflare/agents `docs/codemode/approvals.md`, `docs/codemode.md:425-429`, `packages/codemode/README.md`, `packages/codemode/src/connectors/toolset.ts` (upstream ~2026-06-17, newer than our pin).
- [[ALW-347]] — RBAC spine that dropped `needsApproval` and surfaced this.

## Discussion

**🤖 mcp** · agent · 2026-07-06 21:49

> From [[ALW-398]] AC-5 (org-agent tool audit): the Think 0.11 upgrade brings first-class **durable tool approvals** we should reconcile here rather than adopt piecemeal. Relevant surfaces now available at `@cloudflare/think@0.12.1`:
> 
> - `needsApproval: true` tools pause durably; built-in callables `pendingExecutions()` / `approveExecution(id)` / `rejectExecution(id, reason?)` (approval UIs must render args from `pendingExecutions()`, not the truncated transcript).
> - 0.11 `durable-pause` approval descriptors park in `cf_think_action_pending_approvals` and resume **connection-independently** (approve from a dashboard with no live socket); `ActionApprovalDescriptor` is unified across durable-pause / codemode / approval-gated parts; `pendingApprovals()` lists all.
> - `action()` + `getActions()` + a durable action ledger (replay settled outputs by idempotency key).
> 
> ALW-398 deliberately **did not** adopt these (kept the tool set lean); this task is the right home. The org agent is now on 0.12.1, so the pattern is available to build against.


**🤖 mcp** · agent · 2026-07-08 06:09

> PR: https://github.com/sfab-oss/sfab-starter/pull/44
> 
> Key finding that reframes this task: the premise (pinned think@0.8.5 / codemode@0.3.8 silently stripping `needsApproval`) is outdated — **ALW-398 already upgraded us to think@0.12.1 / codemode@0.4.2**, which ships the durable-approval runtime, and `getTools()` already uses the new `createExecuteTool`. So this became a wire-up + gate + prove + document task, not a plumbing upgrade.
> 
> AC coverage:
> - **AC-1** (behavior, verified against installed dist): `createExecuteTool` builds a `ToolSetConnector` (not the legacy stripping `createCodeTool`); it maps `needsApproval → requiresApproval:true` — preserved, not stripped; a gated codemode call durably pauses. Locked by a test that runs a real `ToolSetConnector`.
> - **AC-2** (upgrade decision): no upgrade needed — the runtime is already present (ALW-398). Recorded in the PR + `docs/guides/agent-tool-approvals.md`.
> - **AC-3** (per-tool-class rule): documented in the new guide + mirrored on `getOrgAgentTools`. Three classes: autonomous+RBAC / top-level human approval / no-agent-tool.
> - **AC-4** (one gated write): `delete-product` hoisted top-level with `needsApproval: true` (RBAC still enforced post-approval); test proves it's exposed top-level + excluded from codemode. Live model-driven turn is a `pnpm dev` manual check (harness can't spawn facets/model — same limit as org-agent.workerd.test.ts).
> - **AC-5** (UI): the dormant Approve/Reject prompt in `default-tool.tsx` is now live for `delete-product`.
> - **AC-6**: typecheck + lint + full test suite pass (227 web + 27 registry).
> 
> Boundaries respected: `assertCan` RBAC unchanged; no money/document mutation exposed; no `needsApproval` reintroduced on a codemode tool without confirming the installed version pauses (it does). Gating a currently-autonomous tool (delete-product) was confirmed with the requester before implementing.

