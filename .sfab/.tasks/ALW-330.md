---
id: task_NHRJXu4AWteRcPq0foCXC
number: 330
display_id: ALW-330
title: "sfab-starter: documentation content + ADR cleanup & re-enumeration (knowledge-layer E1–E4)"
status: review
kind: deliverable
priority: high
needs_walkthrough: false
created: 2026-06-18T19:52:02.307Z
updated: 2026-06-18T20:21:31.332Z
comment_count: 0
parent_task_id: task_KhzVQHNrUuhvws2eB2d51
---

# ALW-330 sfab-starter: documentation content + ADR cleanup & re-enumeration (knowledge-layer E1–E4)

## Context

The docs / knowledge-layer exploration (grill-with-docs, 2026-06-18) locked decisions **E1–E4** for how `sfab-starter` structures documentation for BOTH human developers and AI agents. This task **lands the content** and **cleans up the existing, partly-misleading docs**. It is the "content" half that **[[ALW-326]]'s Non-goals explicitly defer** ("the formal `docs/architecture` doc + ADRs are gated by the docs-structure exploration" — now complete). ALW-326 scaffolds the empty structure; this task fills it and sets the governing convention. Pairs with the platform task that folds E1–E4 into ADR-0010 §H.

### Locked decisions (E1–E4)
- **E1 — Keep decision records under a STRICT significance bar.** An ADR is warranted only when a choice is (a) costly/disruptive to reverse, (b) cross-cutting, AND (c) chose among real alternatives. Reject "ADRs for everything." Records capture opinions + reversals (supersede model).
- **E2 — Five-bucket taxonomy:** `README.md` (adopter launchpad) · `AGENTS.md` (Tier-1 router) · `docs/architecture.md` · `docs/decisions/` · `docs/guides/` (renamed from `template-best-practices/`). **DROP `docs/plans/`** — transient plans live in the SFAB task system or Claude Code plan-mode, never `docs/`.
- **E3 — `AGENTS.md` is the single Tier-1 source; `CLAUDE.md` is a symlink to it** (identical content, zero drift); tool-specific divergence later (`.cursor/rules`) is its own file. No human/agent doc split. Skills = on-demand Tier-2 (vendored hash-pinned + authored), discovery via `description`. Guide-vs-skill boundary. Reference = generated (Tier-3).
- **E4 — Starter knowledge lives in-repo `docs/`** (PR-authored, repo is source of truth), NEVER `.sfab/.docs`.

## Scope

### A. ADR cleanup + clean re-enumeration (start clean)
- **Principle (user):** a superseded / no-value ADR is **REMOVED and the sequence RE-ENUMERATED** (one-time template reset), not kept as a tombstone.
- **Remove:** ADR-002 (t3-env — obsolete; describes Next.js + Postgres, replaced by the cloudflare-env approach) and ADR-005 (drizzle-zod — only *Proposed*, reversed by [[ALW-327]] D2/D3).
- **Proposed clean set (finalize numbering at execution — ⚠️ ask first):**
  - `ADR-001` — Monorepo & layer-sliced architecture (rewrite old-001 free of Next.js framing; fold in ALW-327 D1/D6: layer-sliced feature-keyed model, role-over-tech renames, one-app).
  - `ADR-002` — D1 migrations (old-003 renumbered; light `web`/`db` rename touch-ups).
  - `ADR-003` — Cloudflare env + db singleton (old-004 renumbered; amend for `cloudflare-env`→`env`, `db-d1`→`db`; affirms D5).
  - `ADR-004` — Schema sources & boundary types (NEW — D2/D3: two independent sources, no drizzle→zod, direction-split types, ISO `text` timestamps, money customType; replaces old-005).
  - `ADR-005` — Documentation & knowledge-layer convention (NEW — records E1–E4 itself; qualifies under its own bar).
- Update all inbound cross-refs to the new numbers (incl. [[ALW-326]] References + `AGENTS.md`).

### B. New content
- **`docs/architecture.md`** — the single explanation map: layer-sliced model + the worked example (db → contract → core → Hono API/MCP → components) + guiding-principles intro. Written now as the **TARGET SPEC** (docs-first); [[ALW-326]] restructures code to match (spec briefly leads code — accepted).
- **Revise `docs/decisions/template.md`** — add the E1 significance bar up front; lighten the heavy Pros/Cons ceremony; add a supersedes/reversal field.

### C. Knowledge-layer convention wiring (content, not scaffold)
- Rename `docs/template-best-practices/` → `docs/guides/`; carry its index + decisions cross-link. `guides/` IS the "database-smells house style" (why + preferred pattern + `path:line` refs). *(Migrating + pruning the existing platform engineering docs into guides is a FOLLOW-UP, not this task.)*
- **`AGENTS.md` = Tier-1 router content:** commands + conventions + index into architecture/decisions/guides/skills; remove `docs/plans` references.
- **`CLAUDE.md` → symlink to `AGENTS.md`** (reconcile root `AGENTS.md` vs the drifted `.claude/CLAUDE.md`; remove the duplicate).

**Demarcation from [[ALW-326]]:** 326 owns the empty folder skeleton, `apps/docs` renderer wiring, `.agents/skills` symlink scaffolding, package renames. THIS task owns doc CONTENT + ADRs + `template.md` + `AGENTS.md` router content + `CLAUDE.md` symlink + `guides/` naming + the E1–E4 convention.

## Acceptance criteria
- [ ] AC-1: ADR-002 (t3-env) and ADR-005 (drizzle-zod) removed; `docs/decisions/` re-enumerated into a clean contiguous sequence; no dangling cross-refs. verify: `ls docs/decisions`; grep old numbers.
- [ ] AC-2: ADR set reflects current reality — old-001 rewritten free of Next.js; migrations + env/singleton ADRs renumbered + rename-corrected. verify: `grep -ri "next.js\|postgres" docs/decisions` returns nothing.
- [ ] AC-3: A new ADR records the schema-sources/boundary-types decision (D2/D3). verify: read.
- [ ] AC-4: A new ADR records the documentation / knowledge-layer convention (E1–E4). verify: read.
- [ ] AC-5: `docs/architecture.md` exists with the layer-sliced model + worked example + principles intro. verify: read.
- [ ] AC-6: `template.md` carries the E1 significance bar + supersedes field + lighter ceremony. verify: read.
- [ ] AC-7: `docs/template-best-practices/` renamed to `docs/guides/` with index + cross-link intact. verify: tree.
- [ ] AC-8: `AGENTS.md` is the Tier-1 router (index into docs+skills, no `docs/plans` ref); `CLAUDE.md` is a symlink to it; no second drifted entry file. verify: `ls -la` shows the symlink; grep `AGENTS.md` for `plans`.
- [ ] AC-9: E1–E4 reconciled with platform ADR-0010 §H (paired task) — no contradiction.

## Non-goals
- `apps/docs` renderer wiring — [[ALW-326]] (scaffold).
- Package renames / layer restructure / primitive skeleton — [[ALW-326]] / [[ALW-327]].
- Migrating + pruning platform engineering docs (code-smells / database-smells) into starter guides — FOLLOW-UP task.
- The self-maintained Biome ruleset; the UI composition idiom — separate deferred explorations.
- Implementing the architecture the docs describe — [[ALW-326]] implements to match.

## Boundaries
- ✅ Always: docs content only; match E1–E4 + [[ALW-327]] D1–D10; in-repo `docs/` (never `.sfab/.docs`).
- ⚠️ Ask first: the exact final ADR numbering/mapping; deleting any doc with residual value.
- 🚫 Never: route starter docs through `.sfab/.docs`/MCP; reintroduce `docs/plans`; "ADR for everything."

## References
- [[ALW-327]] D1–D10 (directory/package structure) · [[ALW-326]] scaffold (this fills it) · [[ALW-323]] Group.
- Platform **ADR-0010 §H** (knowledge layer) — paired amendment task · ADR-0017.
- Local design log: `docs/.local/2026-06-17-2011-template-strategy-and-factory-process/docs-knowledge-layer/` (E1–E4 + 4 research reports).
- Existing starter docs to clean: `decisions/001–005`, `template.md`, `template-best-practices/`, `AGENTS.md`, `.claude/CLAUDE.md`.
