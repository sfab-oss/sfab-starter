---
id: task_pzUSNdTvK4dVmhXHQbq3W
number: 429
display_id: ALW-429
title: "Command-K palette: wire real global search (pages + catalog + documents)"
status: review
kind: deliverable
priority: medium
needs_walkthrough: true
created: 2026-07-08T20:04:16.955Z
updated: 2026-07-09T00:46:04.332Z
comment_count: 2
last_comment_at: 2026-07-09T00:46:31.717Z
assignee_id: i2dpBuXJnKvwD1yro1adgfw5hJ58i5Gv
---

# ALW-429 Command-K palette: wire real global search (pages + catalog + documents)

## Context

The ⌘K palette (`components/search/search-command.tsx`) looks finished but is still a stub for entity search. [[ALW-423]] QA (2026-07-08) confirmed the **static page list is already complete** (Home, Catalog, Entities, Documents, Settings) — earlier notes that Documents was missing are stale. Remaining gaps: no product/document entity search; dead scaffolding (`isLoading` hardcoded `false`, a 300ms debounce that only feeds a local string filter); the query string **persists** when the dialog closes and reopens (finding S-2); and the **search input chrome looks wrong** (finding S-7) — a short pill nested inside an extra `border-b` flex wrapper, so it doesn’t span the dialog width.

For an AI-native ERP base the palette should be the fast path to any record.

## Scope

- Keep the static page list derived from the nav definition (already includes Documents; avoid duplicating a second hardcoded list).
- Wire live entity search: products via the existing catalog search endpoint; documents (folio/entity name) via a search-capable endpoint (add one if the documents API lacks it).
- Grouped results (Pages / Products / Documents) with icons, loading state, empty state, and Enter-to-navigate to the right detail route.
- Reset `query` / `debouncedQuery` when the dialog closes (`onOpenChange(false)`).
- Either wire `isLoading` to real async work or remove the dead scaffold.
- Fix search-input layout: drop the nested pill / extra header wrapper so `CommandInput` spans the dialog like a normal cmdk header (full-width, no double chrome).
- Keep keyboard behavior (⌘K, Ctrl-K, `/`, input-focus guard) intact; keep response snappy (debounce + request cancellation).

## Acceptance criteria

- [ ] AC-1: Typing a product name surfaces catalog results that navigate to `/catalog/$id`.
- [ ] AC-2: Typing a document folio or entity name surfaces document results that navigate to `/documents/$id`.
- [ ] AC-3: All app pages, including Documents, appear as page results.
- [ ] AC-4: Loading, empty, and no-results states render correctly; the `isLoading` scaffold is either real or removed.
- [ ] AC-5: Keyboard-only use works end-to-end (open, navigate results, select, close).
- [ ] AC-6: Closing and reopening the palette shows an empty query (no sticky previous search). verify: type a query, Esc, ⌘K again.
- [ ] AC-7: Search input spans the dialog header cleanly (no short nested pill / double border chrome). verify: screenshot of open palette.

## Non-goals

- Command/action execution from the palette (create product, etc.) — possible follow-up.
- Full-text search infrastructure; simple prefix/LIKE search is fine for the base.

## Boundaries

- ✅ Always: org-scoped queries only; reuse existing hono route patterns.
- ⚠️ Ask first: new DB indexes or schema changes for search.
- 🚫 Never: cross-org leakage in results.

## References

- `apps/web/src/components/search/search-command.tsx`, `apps/web/src/hono/org-protected/catalog/search.ts`
- QA feed: [[ALW-423]] AC-3 / findings S-2, S-3, S-7 — `active/alw-422-starter-qa-sweep/artifacts/ISSUES.md`
- Evidence: `artifacts/browser-qa/alw423-20260708-desktop/screenshots/13-palette-metak.png`

## Discussion

**🤖 local-impl** · agent · 2026-07-09 00:34

> Implementing Raycast-style ⌘K palette on branch `alw-429-command-k-palette`.
> 
> Plan:
> - Extract shared `platformNavigationItems` for Pages group
> - Wire live search: catalog `/search`, documents list `search`, entities list `search`
> - Grouped results (Pages / Products / Documents / Entities), debounce 300ms, `shouldFilter={false}`
> - Fix chrome: full-width Raycast input, reset query on close, real loading state
> 
> No code changes on the QA worktree; edit-only on this branch.


**🤖 local-impl** · agent · 2026-07-09 00:46

> Implementation landed on draft PR https://github.com/sfab-oss/sfab-starter/pull/49 (`d05a560`).
> 
> ### What shipped
> - Raycast-style ⌘K: Pages (shared `platformNavigationItems`) + live Products (`/catalog/search`), Documents, Entities
> - Full-width command input (no nested pill); query resets on close
> - Debounced async search (≥2 chars), grouped results, real loading spinner
> 
> ### Verification
> - `pnpm typecheck` + pre-commit turbo build/test green (236 web tests)
> - Browser QA blocked locally: zombie vite still holding `:4011` / TanStack bus `:42085` (PID 3312) from the ALW-423 session — please kill that process, then I can re-shot AC-1..7
> 
> Ready for your skim / merge when green.

