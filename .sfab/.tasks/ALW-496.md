---
id: task_XoAqa_Y6OZv6T0XJKXBQn
number: 496
display_id: ALW-496
title: "Smell lint gates (useEffect justify) — after Ultracite exit"
status: ready
kind: deliverable
priority: low
needs_walkthrough: false
created: 2026-07-10T18:03:38.889Z
updated: 2026-07-23T05:50:38.905Z
comment_count: 0
parent_task_id: task_UzXjI_QTeAYFb60mKKJxU
---

# ALW-496 Smell lint gates (useEffect justify) — after Ultracite exit

## Context

After Ultracite exit ([[ALW-495]], [[ALW-498]]), enforce two mechanical smell gates in Biome on the platform repo. Agents and humans justify exceptions inline; AI review only judges what lint cannot.

## Scope

- **No casual `useEffect` / `useLayoutEffect`:** own GritQL Biome plugin at **error**. Exclude stock shadcn under `packages/ui/src/components/shadcn/**`. Every other call site: refactor to the preferred pattern (`code-smells.md`) **or** `// biome-ignore lint/plugin/…: <justification>` for genuine external sync.
- **Raw `console.*` ban (server):** enable Biome `noConsole` for server-side trees; allow only `@sfab/log` helper module (`packages/log/src/structured-log.ts`). Mirror [[ALW-662]] exclusion intent for client UI / docs / harness-cursor / runner until those migrate. Fold `scripts/check-no-raw-console.mjs` into Biome so `pnpm lint:check` has **one** enforcement point (remove or reduce the script to a thin self-test if still useful).
- Cross-link `docs/engineering/code-smells.md` + `docs/engineering/structured-logging.md`.

## Acceptance criteria

- [ ] AC-1: Biome plugin bans `useEffect`/`useLayoutEffect` at error outside shadcn; `pnpm lint:check` fails on a new unignored call site. verify: add then remove a throwaway call in a non-shadcn file
- [ ] AC-2: Existing non-shadcn effect call sites are either refactored or have a justified `biome-ignore`. verify: `pnpm lint:check` green
- [ ] AC-3: Biome `noConsole` (or equivalent override set) enforces server-side structured logging; helper module exempt; client carve-outs match ALW-662 intent. verify: `pnpm lint:check` green; raw console in a server file fails
- [ ] AC-4: `scripts/check-no-raw-console.mjs` is removed from the primary `lint:check` path (deleted or demoted). verify: `package.json` `lint:check` script

## Non-goals

- React Doctor rules / CI wiring.
- Encoding DB/migration smells in Biome.
- Migrating harness-cursor / runner onto `@sfab/log` (exclusions stay until follow-ups).
- Starter ruleset copy (follow-up after platform lands).

## Boundaries

- ✅ Always: justify every remaining effect; never log secrets; keep `@sfab/log` as the only server console emitter.
- ⚠️ Ask first: broadening shadcn / client exclusions.
- 🚫 Never: weaken to warn-only for the useEffect gate without owner say-so.

## References

- [[ALW-494]] · [[ALW-498]] · [[ALW-662]] · `docs/engineering/code-smells.md` · `docs/engineering/structured-logging.md`
