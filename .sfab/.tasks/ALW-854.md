---
id: task_SOpOwIHrPgPhJ8XJUkWP5
number: 854
display_id: ALW-854
title: Delete the committed .sfab/.tasks mirror in sfab-starter
status: draft
kind: deliverable
priority: medium
created: 2026-09-01T01:44:13.962Z
updated: 2026-09-01T01:44:13.962Z
comment_count: 0
---

# ALW-854 Delete the committed .sfab/.tasks mirror in sfab-starter

## Context
Same as the platform's mirror deletion: after the git-sync rip, `.sfab/.tasks/` in the starter is a stale copy of the board. Public clones are already told to `rm -rf .sfab`; the template should not ship it. `.sfab/template.json` / `fabrication.json` stay (fabricate provenance).

## Scope
- `git rm -r .sfab/.tasks` (+ `INDEX.md`; `.sfab/.docs` if present).
- Update `docs/template-init.md` / `AGENTS.md` wording that mentions `.sfab/.tasks`.

## Acceptance criteria
- [ ] AC-1: `.sfab/.tasks` no longer exists on `main`; `.sfab/template.json` and `fabrication.json` unchanged. verify: `ls .sfab`
- [ ] AC-2: No doc instructs reading `.sfab/.tasks`. verify: `rg "\.sfab/\.tasks" docs AGENTS.md`
- [ ] AC-3: Starter CI green (incl. `i18n:lint`, registry `generate:check`).

## Non-goals
Any product code change.

## Boundaries
- ✅ Always: one small PR after the platform rip merges.
- ⚠️ Ask first: nothing expected.
- 🚫 Never: delete `.sfab/` wholesale (fabricate reads it).

## References
- Blocked by the platform git-sync rip task.
