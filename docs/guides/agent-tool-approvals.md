# Agent tool approvals (moved)

Approval-gated writes and the full ERP tool authoring guide now live in
**[writing-agent-tools.md](./writing-agent-tools.md)** (ALW-524).

That doc covers `needsApproval` ↔ codemode `requiresApproval`, the per-tool-class
rule (autonomous / approval / no tool), and how to add gated writes such as
`delete_product`.
