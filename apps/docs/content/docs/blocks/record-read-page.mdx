---
title: Record read page
description: Full-route invoice record — PropertyGrid and activity log on a shell page.
---

Full **read** route for an open invoice: shell header with breadcrumbs, `PropertyGrid`
summary, and `ActivityTimeline` activity log. No dialog — this is the `/documents/$id` style
record view.

<BlockViewer name="record-read-page" />

## What it demonstrates

- **`InvoiceRecordView`** — read-only property grid + activity log on a page.
- **Shell header** — Collect → folio breadcrumb with status badge and customer name.

## Source

`packages/registry/registry/blocks/record-read-page/` — open at
[`/view/record-read-page`](/view/record-read-page).
