---
title: Spreadsheet viewer page
description: Full-route .xlsx document — a read-only spreadsheet on a shell page.
---

Full **document** route: shell header with breadcrumbs and actions, hosting the
`SpreadsheetViewer` — a read-only `.xlsx` reader (styles, merges, formulas) that
fills the page below the header.

<BlockViewer name="spreadsheet-viewer-page" />

## What it demonstrates

- **`SpreadsheetViewer`** — a read-only workbook sized to fill the shell content area.
- **Shell header** — Documents breadcrumb with a Download action.

## Source

`packages/registry/registry/blocks/spreadsheet-viewer-page/` — open at
[`/view/spreadsheet-viewer-page`](/view/spreadsheet-viewer-page).
