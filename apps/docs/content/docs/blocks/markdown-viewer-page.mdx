---
title: Markdown viewer page
description: Full-route markdown document — a read-only editor on a shell page.
---

Full **document** route: shell header with breadcrumbs and actions, hosting the
`MarkdownEditor` in read-only mode. The same surface that authors markdown also
displays it — drop `readOnly` to make the page editable.

<BlockViewer name="markdown-viewer-page" />

## What it demonstrates

- **`MarkdownEditor` (read-only)** — a document rendered on a centered, scrollable page column.
- **Shell header** — Documents breadcrumb with a Download action.

## Source

`packages/registry/registry/blocks/markdown-viewer-page/` — open at
[`/view/markdown-viewer-page`](/view/markdown-viewer-page).
