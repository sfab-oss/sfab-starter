---
title: Button
description: Live demo of the shared Button component from @workspace/ui.
---

The docs app registers `@workspace/ui` components in `getMDXComponents()` so MDX pages can render them interactively.

## Default

<Button>Click me</Button>

## Variants

<div className="flex flex-wrap gap-2 not-prose">
  <Button variant="default">Default</Button>
  <Button variant="secondary">Secondary</Button>
  <Button variant="outline">Outline</Button>
  <Button variant="ghost">Ghost</Button>
  <Button variant="destructive">Destructive</Button>
</div>

## Sizes

<div className="flex flex-wrap items-center gap-2 not-prose">
  <Button size="sm">Small</Button>
  <Button size="default">Default</Button>
  <Button size="lg">Large</Button>
</div>
