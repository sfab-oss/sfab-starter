---
title: Badge
description: Status labels in every variant, including icon and count patterns.
---

A second `registry:ui` entry, to show the pattern is repeatable. Demo at
`apps/docs/src/registry/demos/badge-demo.tsx`, registered alongside Button — open
it full screen at [`/view/badge`](/view/badge).

<ComponentPreview name="badge" />

## Usage

```tsx
import { Badge } from "@workspace/ui/components/shadcn/badge";

<Badge variant="secondary">Verified</Badge>;
```

## Variants

`default` · `secondary` · `destructive` · `outline`.
