/**
 * Skeleton — EntityContact (Tier-1 base primitive, ADR-0010 §E).
 *
 * Organizations + People, polymorphic, custom-field-ready.
 *
 * Not yet implemented. When built it follows the layer-sliced, feature-keyed
 * shape (same `contacts` key in every layer): `packages/db/src/schema/contacts.ts`,
 * `packages/contract/src/contacts/`, `packages/core/src/contacts/`,
 * `apps/web/src/hono/<auth-scope>/contacts/`, `packages/agent/src/tools/contacts/`,
 * `apps/web/src/components/contacts/`. See `docs/architecture.md`.
 */
export type EntityContactSkeleton = never;
