// Skeleton — EntityContact tables (Tier-1 base primitive, ADR-0010 §E).
// entities (Organizations + People), polymorphic + custom fields.
// Not yet implemented and deliberately NOT exported from schema/index.ts until
// it is. See packages/core/src/contacts/ and docs/architecture.md.
export type EntityContactTablesSkeleton = never;
