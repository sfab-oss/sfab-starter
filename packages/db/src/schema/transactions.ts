// Skeleton — TransactionCore tables (Tier-1 base primitive, ADR-0010 §E).
// documents + line_items; decimal-safe money + generic tax + numbering/status.
// Not yet implemented and deliberately NOT exported from schema/index.ts until
// it is. See packages/core/src/transactions/ and docs/architecture.md.
export type TransactionCoreTablesSkeleton = never;
